{
     let branch= "#"
    for (let count = branch; count < branch+branch; count++)
        console.log(count);
    for (let count = branch+branch; count < branch+branch+branch; count++)
        console.log(count); 
    for (let count = branch+branch+branch; count < branch+branch+branch+branch; count++)
        console.log(count);
    for (let count = branch+branch+branch+branch; count < branch+branch+branch+branch+branch; count++)
        console.log(count);
    for (let count = branch+branch+branch+branch+branch; count < branch+branch+branch+branch+branch+branch; count++)
        console.log(count);
    for (let count = branch+branch+branch+branch+branch+branch; count < branch+branch+branch+branch+branch+branch+branch; count++)
        console.log(count);
    for (let count = branch+branch+branch+branch+branch+branch+branch; count < branch+branch+branch+branch+branch+branch+branch+branch; count++)
        console.log(count);
    for (let count = branch; count < branch+branch; count++)
        console.log(count);
}
